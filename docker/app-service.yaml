apiVersion: v1
kind: Service
metadata:
  name: appprodutos-app-service
  namespace: appprodutos
spec:
  type: NodePort
  selector:
    app: appprodutos-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080